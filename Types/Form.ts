type FormHandleChange = ({name, value}: {name: string, value: string|string[] }) => void;

/*
type PanelState = {
    x: number;
    y: number;
    width:number;
    height: number;
    zIndex?: number;
}
*/

export {
	FormHandleChange,
    // PanelState,
}